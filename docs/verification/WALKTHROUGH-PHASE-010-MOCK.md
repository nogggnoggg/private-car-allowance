# 人類檢核走查腳本 — PHASE-010 Mock Gate（稽核列表／篩選）

| 項目 | 內容 |
|---|---|
| Walkthrough ID | WALKTHROUGH-PHASE-010-MOCK |
| 建立日期 | 2026-08-09 |
| 檢核對象 | 前端稽核檢視頁（`/admin/audit-logs`，PHASE-010-T7 `15a1a9a`） |
| 檢核性質 | Mock Gate（本機 dev 環境＋合成資料目視；**非**容器整合驗收） |
| 環境 | backend-dev＋frontend-dev（大總管於 Gate 前啟妥並播種素材；`.env` 沿 KNOWN_ISSUES O-2 既有 STORAGE_ROOT） |
| 檢核結果 | **第一輪未通過（人類 leonchih 2026-08-09，逐筆表列制）**——兩項反饋：MG-1 改前/改後結構與內容對不上（{to,from} JSON 整包塞改後欄/油耗巢狀值落錯欄/英文枚舉值不可讀）；MG-2 日期篩選以 UTC 日為界與台灣時間顯示不一致（篩 8/7-8/8 出現畫面 8/9 列）。**兩項修法同日裁定**：MG-1=智能攤平＋值中文化／MG-2=以台灣日曆日為準。修復後重上第二輪。 |

## 這次要看什麼（30 秒版）

管理員多了一頁「稽核紀錄」：誰、什麼時候、對誰、做了什麼、**改前改後是什麼**。
**驗收核心只有一件事（AD-US-14③）：「改前改後」那一欄，不懂技術的人看不看得懂。**
其餘（篩選、翻頁、五態文案）為常規檢查。

## 前置（大總管於 Gate 前完成，您不用做）

- dev DB 已播種：至少各一筆——建立帳號、停用＋啟用、密碼重設、代建草稿、作廢（含中文原因）、參數版本（三型）、油耗版本（含異動前後）。
- 兩個測試帳號：管理員 `e2eadmin`、一般使用者一名（密碼由大總管當場提供，全合成）。

## Step-by-step

### A. 入口與權限（2 分鐘）

1. 以**管理員**登入 → 首頁管理員區塊應有「**稽核紀錄**」入口 → 點入。
2. 預期：列表載入，每列四要素——**操作時間**（台北時間，不是 `2026-08-09T06:00:00Z` 這種原始格式）／**操作者**（顯示名稱，不是一串 ID）／**操作類型**（中文，如「作廢申請」，不是 `APPLICATION_VOIDED`）／**受影響資料**。
3. 登出，改以**一般使用者**登入 → 首頁**不應**出現「稽核紀錄」入口；直接在網址列輸入 `/admin/audit-logs` → 應顯示無權限訊息、**看不到任何稽核內容**。

### B. 改前改後摘要——驗收核心（5 分鐘）

以管理員回到稽核頁，逐一找到下列事件並展開看「欄位／改前／改後」表格：

| 找哪筆 | 應該看到 | 不能看到 |
|---|---|---|
| 油耗資料異動 | 「異動前版本」「異動後版本」兩列（內容為油種/油耗數值），加「依據備註」一列 | `[object Object]`；欄位名顯示成「改前」和表頭撞名 |
| 作廢申請 | 「作廢原因」一列，中文原因**逐字**可讀 | 原因被截斷成看不懂、或顯示英文鍵名 `reason` |
| 密碼重設 | 只有「須變更密碼」一列，值為 true | **任何**看起來像密碼的東西 |
| 停用帳號 | 啟用狀態的變化（from/to） | 亂碼或空白 |
| 參數版本建立 | 參數類型與數值欄位（中文標籤） | — |

4. **總結判斷**：這張表拿給完全不懂系統的同事看，能不能說出「誰把什麼從 A 改成 B」？能＝過。

### C. 篩選與分頁（3 分鐘）

5. 用「操作類型」下拉選一類（如「作廢申請」）→ 列表收斂到只剩該類。
6. 設日期區間（起訖同一天，選有事件的那天）→ 該天事件在列；改成無事件的日期 → 顯示「查無稽核紀錄。」（注意：與完全沒資料時的「尚無任何稽核紀錄。」是兩句不同的話）。
7. 清掉篩選，翻到第 2 頁再套任一篩選 → 應自動回到第 1 頁。
8. 第 1 頁時「上一頁」應為灰色不可按；最末頁時「下一頁」灰色不可按。頁腳有「第 N 頁／共 M 筆」。

### D. 版面極端值（2 分鐘）

9. 找一筆**長中文作廢原因**的列 → 版面不應被撐爆或水平捲動。
10. 瀏覽器視窗縮到手機寬（約 375px）→ 列表仍可讀、無水平溢位。（正式 375px 驗證在 T8 E2E，此處目視即可。）

### E. 錯誤態（1 分鐘，可選）

11. 大總管當場停掉 backend-dev → 重新整理稽核頁 → 應顯示錯誤訊息＋「重試」鈕；重啟後按重試恢復。

## 這期不會做的事（先講清楚）

- 稽核資料的清理/保留期限、匯出——不在範圍（PHASE-011 之後再議）。
- 一般使用者看自己的稽核紀錄——需求文件沒要求。
- 此頁只讀不寫：任何操作都不會產生新的稽核（查詢本身不記稽核，是刻意設計）。

## 揭露事項（Gate 時請知悉；期中複審 #2 關閉確認最終版）

1. **標籤字面之業務用詞**：31 個中文欄名＋10 個操作類型標籤（如「本次實際保養費用」「代建立申請」）為本期實作首次擬定，Spec 未逐字指定——若您對任一用詞有意見，現在改成本最低。
2. 油耗異動的「異動前版本/異動後版本」內容顯示為 JSON 樣式字串（如 `{"fuelType":"GASOLINE_95",...}`）——D3=(c) 已裁定之已知代價；欄名有中文化、值沒有。
3. 異動明細表為瀏覽器預設樣式（未加專屬美化）；長值於窄螢幕以橫向捲動呈現。
4. 篩選的使用者下拉（SF-1 裁定落地）：選項文字為「顯示名稱（登入帳號）」；**未標示已停用帳號**；清單全量、無搜尋框——帳號很多時下拉會很長。
5. 下拉清單載入失敗時的降級文案：「使用者清單載入失敗，暫時無法依使用者篩選；其餘篩選與稽核列表不受影響。」——是否可接受。
6. 手動改網址把頁次帶超過總頁數時，會顯示「尚無任何稽核紀錄。」且無分頁鈕可退回（正常操作到不了這裡）。
7. **時區（已裁定，勿重議）**：稽核頁時間為瀏覽器本機時區、列印版為固定台北——您已於 2026-08-09 裁定接受（Spec §17.1 #10）。

## 第二輪檢核（MG-1/MG-2 修復後；素材由大總管重播種）

**修復驗收項（正對第一輪兩筆問題）：**
1. 油耗異動列：應為 **4 列**（油種／油耗（公里／公升）／生效日／依據備註），每列改前→改後對得上；油種顯示中文（如「95 無鉛汽油」）。
2. 停用/啟用列：「啟用狀態」一列兩欄，改前「是」→改後「否」（或反向）——不再是 JSON 整包。
3. **油耗首版列**（改前欄全為「—」）：這是您第一輪截圖的案例——三子鍵仍逐列拆開。此為裁定射程的擴張半邊，請獨立確認。
4. **日界驗收**：篩台灣日 X~Y，畫面不再出現 X-1 或 Y+1 的列；素材含台北 8/8 23:5x 與 8/9 00:0x 兩筆邊界列，篩 8/8 應含前者不含後者。

**新增確認項：**
5. **★ 日期形式並存確認**：同一張明細表內「生效日＝`2026-08-01`」（純日期原樣）與「作廢時間＝`2026/8/9 上午…`」（在地化）兩種形式並存——裁定的「日期在地化」是否即指此現況（純日期維持 YYYY-MM-DD）？否決則追加純日期在地化。
6. 值中文化字面：差旅補助/保養分攤/折舊補貼、92/95/98 無鉛汽油/柴油、管理員/一般使用者、油資參數（油價）/ETC 參數/折舊參數（取自設定頁節標題用語）、是/否——用詞是否合乎業務語彙。
7. `kmPerLiter` 值顯示四位小數（`12.3400`）——是否可接受（後端既有字串化）。

**維持事項（已裁定勿重議）**：時區顯示＝瀏覽器本機；國外登入篩選以台灣日為準（§17.1 #10/#11）。
**其餘沿第一輪揭露**：明細表預設樣式／下拉無已停用標示無搜尋／越界空頁。

| 第二輪檢核結果 | **（待填）** |
|---|---|

## 之後的 Gate（預告）

- 整合 Gate：容器內端到端（做一次真實管理操作 → 稽核頁查得到；拒刪真實畫面）→ `WALKTHROUGH-PHASE-010-INTEGRATION.md`
- PR 合併批准：人類決策，不授權代行
